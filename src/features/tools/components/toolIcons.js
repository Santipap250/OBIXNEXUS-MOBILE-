import { Activity, Battery, Cpu, Gauge, Layers, Radar, Shield, Wifi } from "lucide-react";

export const TOOL_ICONS = Object.freeze({
  activity: Activity,
  battery: Battery,
  cpu: Cpu,
  gauge: Gauge,
  layers: Layers,
  radar: Radar,
  shield: Shield,
  wifi: Wifi,
});
